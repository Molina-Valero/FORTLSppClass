# ── Point cloud processing ────────────────────────────────────────────────────
laspy==2.7.0
lazrs>=0.6.0          # LAZ decompression backend for laspy
numpy==2.4.2
scipy>=1.9.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib==3.10.8
pillow==12.1.1
pandas>3.0.0

# matplotlib sub-dependencies (pinned for reproducibility)
contourpy==1.3.3
cycler==0.12.1
fonttools==4.61.1
kiwisolver==1.4.9
packaging==26.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0


# ── Geometric features (TreeProjection_features.py) ───────────────────────────
# Cython extension — requires a C compiler (gcc / MSVC) and scipy at build time.
# pip will compile from source if no pre-built wheel exists for your platform.
jakteristics==0.6.2

# ── YOLOv8 training (train_classifier.py) ─────────────────────────────────────
# Install torch separately BEFORE running pip install -r requirements.txt.
# Choose ONE of the commands below depending on your setup:
#
#   CPU-only:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#
#   CUDA 12.x:
#     pip install torch --index-url https://download.pytorch.org/whl/cu128
#
torch>=2.0.0
torchvision>=0.21.0
ultralytics>=8.0.0
